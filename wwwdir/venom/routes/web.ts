import type { Express } from "express";

/** Non-API routes (e.g. server-rendered pages) — reserved for future use. */
export function registerWebRoutes(_app: Express): void {
  // intentionally empty
}
